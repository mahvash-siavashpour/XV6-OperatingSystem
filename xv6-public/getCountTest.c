#include "types.h"
#include "stat.h"
#include "user.h"


int main(int argc, char *argv[]){
    getpid();
    getpid();
    // printf(1,"%d",argv[argc-1]);
    int y = 11;
    int x =getCount(getpid(),y);
    printf(1,"%d",x);


exit();
}